<Window x:Class="WindowsHoneypot.UI.Views.ReplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:vm="clr-namespace:WindowsHoneypot.UI.ViewModels"
        xmlns:views="clr-namespace:WindowsHoneypot.UI.Views"
        mc:Ignorable="d"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Attack Replay Visualization" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">
    
    <Window.Resources>
        <!-- Converter for position to percentage -->
        <views:PositionToPercentageConverter x:Key="PositionToPercentageConverter"/>
        
        <!-- Style for timeline entry cards -->
        <Style x:Key="TimelineCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAltHighBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        
        <!-- Style for stat cards -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="🎬 Attack Replay Visualization" 
                          FontSize="28" 
                          FontWeight="Bold"
                          Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                <TextBlock Text="Video-style playback of attacker activities" 
                          FontSize="14" 
                          Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                          Margin="0,4,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="📥 Load Replay" 
                       Command="{Binding LoadReplayDataCommand}"
                       Style="{StaticResource AccentButtonStyle}"
                       Margin="0,0,8,0"
                       Padding="16,8"/>
                <Button Content="📄 Export PDF" 
                       Command="{Binding ExportToPdfCommand}"
                       Margin="0,0,8,0"
                       Padding="16,8"/>
                <Button Content="💾 Export Data" 
                       Command="{Binding ExportPlaybackDataCommand}"
                       Padding="16,8"/>
            </StackPanel>
        </Grid>
        
        <!-- Statistics Dashboard -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Mouse Clicks Card -->
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="🖱️" FontSize="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalMouseClicks}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              HorizontalAlignment="Center"
                              Foreground="White"/>
                    <TextBlock Text="Mouse Clicks" 
                              FontSize="12" 
                              HorizontalAlignment="Center"
                              Foreground="White"
                              Opacity="0.9"/>
                </StackPanel>
            </Border>
            
            <!-- Keystrokes Card -->
            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="⌨️" FontSize="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalKeystrokes}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              HorizontalAlignment="Center"
                              Foreground="White"/>
                    <TextBlock Text="Keystrokes" 
                              FontSize="12" 
                              HorizontalAlignment="Center"
                              Foreground="White"
                              Opacity="0.9"/>
                </StackPanel>
            </Border>
            
            <!-- File Operations Card -->
            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="📁" FontSize="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalFileOperations}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              HorizontalAlignment="Center"
                              Foreground="White"/>
                    <TextBlock Text="File Operations" 
                              FontSize="12" 
                              HorizontalAlignment="Center"
                              Foreground="White"
                              Opacity="0.9"/>
                </StackPanel>
            </Border>
            
            <!-- Screenshots Card -->
            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="📷" FontSize="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalScreenshots}" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              HorizontalAlignment="Center"
                              Foreground="White"/>
                    <TextBlock Text="Screenshots" 
                              FontSize="12" 
                              HorizontalAlignment="Center"
                              Foreground="White"
                              Opacity="0.9"/>
                </StackPanel>
            </Border>
            
            <!-- Risk Level Card -->
            <Border Grid.Column="4" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="⚠️" FontSize="32" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding RiskLevel}" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              HorizontalAlignment="Center"
                              Foreground="White"/>
                    <TextBlock Text="Risk Level" 
                              FontSize="12" 
                              HorizontalAlignment="Center"
                              Foreground="White"
                              Opacity="0.9"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Timeline Visualization -->
            <Border Grid.Column="0" 
                   Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                   BorderThickness="1"
                   CornerRadius="8"
                   Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Timeline Header -->
                    <Border Grid.Row="0" 
                           Background="{DynamicResource SystemControlBackgroundAccentBrush}"
                           Padding="16,12">
                        <TextBlock Text="📊 Activity Timeline" 
                                  FontSize="18" 
                                  FontWeight="SemiBold"
                                  Foreground="White"/>
                    </Border>
                    
                    <!-- Timeline List -->
                    <ScrollViewer Grid.Row="1" 
                                 VerticalScrollBarVisibility="Auto"
                                 Padding="12">
                        <ItemsControl ItemsSource="{Binding TimelineEntries}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TimelineCardStyle}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Time -->
                                            <TextBlock Grid.Column="0" 
                                                      Text="{Binding FormattedTime}" 
                                                      FontFamily="Consolas"
                                                      FontSize="12"
                                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,12,0"/>
                                            
                                            <!-- Event Type Badge -->
                                            <Border Grid.Column="1" 
                                                   Background="{Binding SeverityColor}"
                                                   CornerRadius="4"
                                                   Padding="8,4"
                                                   Margin="0,0,12,0">
                                                <TextBlock Text="{Binding FriendlyEventType}" 
                                                          FontSize="11"
                                                          FontWeight="SemiBold"
                                                          Foreground="White"/>
                                            </Border>
                                            
                                            <!-- Description -->
                                            <TextBlock Grid.Column="2" 
                                                      Text="{Binding Description}" 
                                                      TextWrapping="Wrap"
                                                      VerticalAlignment="Center"
                                                      Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Summary Panel -->
            <Border Grid.Column="1" 
                   Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                   BorderThickness="1"
                   CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Summary Header -->
                    <Border Grid.Row="0" 
                           Background="{DynamicResource SystemControlBackgroundAccentBrush}"
                           Padding="16,12">
                        <TextBlock Text="📋 Non-Technical Summary" 
                                  FontSize="18" 
                                  FontWeight="SemiBold"
                                  Foreground="White"/>
                    </Border>
                    
                    <!-- Summary Content -->
                    <ScrollViewer Grid.Row="1" 
                                 VerticalScrollBarVisibility="Auto"
                                 Padding="16">
                        <TextBlock Text="{Binding SessionSummary}" 
                                  TextWrapping="Wrap"
                                  FontFamily="Segoe UI"
                                  FontSize="13"
                                  LineHeight="22"
                                  Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Playback Controls -->
        <Border Grid.Row="3" 
               Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
               BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
               BorderThickness="1"
               CornerRadius="8"
               Padding="16"
               Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Progress Bar -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                              Text="{Binding CurrentPosition}" 
                              FontFamily="Consolas"
                              FontSize="14"
                              VerticalAlignment="Center"
                              Margin="0,0,12,0"/>
                    
                    <Slider Grid.Column="1" 
                           Minimum="0" 
                           Maximum="{Binding TotalEntries}"
                           Value="{Binding CurrentPosition, Mode=TwoWay}"
                           TickFrequency="1"
                           IsSnapToTickEnabled="True"
                           VerticalAlignment="Center"/>
                    
                    <TextBlock Grid.Column="2" 
                              Text="{Binding TotalEntries}" 
                              FontFamily="Consolas"
                              FontSize="14"
                              VerticalAlignment="Center"
                              Margin="12,0,0,0"/>
                </Grid>
                
                <!-- Control Buttons -->
                <StackPanel Grid.Row="1" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    
                    <!-- Previous Button -->
                    <Button Content="⏮️ Previous" 
                           Command="{Binding PreviousEntryCommand}"
                           Padding="12,8"
                           Margin="4"/>
                    
                    <!-- Play Button -->
                    <Button Content="▶️ Play" 
                           Command="{Binding PlayCommand}"
                           Padding="16,8"
                           Margin="4">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Pause Button -->
                    <Button Content="⏸️ Pause" 
                           Command="{Binding PauseCommand}"
                           Padding="16,8"
                           Margin="4">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlaying}" Value="False">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Stop Button -->
                    <Button Content="⏹️ Stop" 
                           Command="{Binding StopCommand}"
                           Padding="12,8"
                           Margin="4"/>
                    
                    <!-- Next Button -->
                    <Button Content="Next ⏭️" 
                           Command="{Binding NextEntryCommand}"
                           Padding="12,8"
                           Margin="4"/>
                    
                    <!-- Speed Controls -->
                    <Border BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                           BorderThickness="1"
                           CornerRadius="4"
                           Padding="8"
                           Margin="16,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="🐌 Slower" 
                                   Command="{Binding DecreaseSpeedCommand}"
                                   Padding="8,4"
                                   Margin="0,0,8,0"/>
                            
                            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">
                                <Run Text="Speed: "/>
                                <Run Text="{Binding PlaybackSpeed, StringFormat='{}{0:F1}x'}" FontWeight="Bold"/>
                            </TextBlock>
                            
                            <Button Content="🚀 Faster" 
                                   Command="{Binding IncreaseSpeedCommand}"
                                   Padding="8,4"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
